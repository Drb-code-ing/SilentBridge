# SilentBridge 预部署检查清单

> 状态：本地产品冲刺已完成核心路径；**尚未部署公网**。  
> 作者提交均为 `DRB-code-ing` 单人作者。

---

## 1. 自动化结果（本轮已跑）

| 检查 | 结果 |
|---|---|
| Web typecheck | ✅ |
| Web unit tests | ✅ 75 passed |
| API typecheck | ✅ |
| API unit tests | ✅ 3 passed |
| Production build (`pnpm build`) | ✅ |
| Playwright E2E 主路径 | ✅ 0 issues |

### E2E 覆盖点

- Landing 医院叙事 + 一键演示 CTA  
- 首页真实使用路径  
- 无障碍：字号档 / 高对比  
- 话术场景筛选（医院）  
- 医院一键演示：字幕 / AI / 风险 / 保存 CTA  
- 当场多轮确认（请对方确认）  
- 确认后仍可保存  
- 多轮时间线  
- 全屏出示  
- 政务场景手动/演示整理链路  
- 记录页复制摘要入口  

---

## 2. 产品能力冻结建议（部署前）

建议把当前 `main` 当作 **初赛候选版**，部署前只修 blocker，不再大改交互。

### 已具备

1. 医院问诊默认评委路径  
2. 药店 / 政务 / 交通场景  
3. 一键演示（无麦克风）  
4. 全屏出示  
5. AI 重点 / 风险 / 确认问题  
6. 当场多轮 + 时间线  
7. 记录保存 / 搜索 / 续聊 / 分享文案  
8. 话术场景化  
9. 无障碍字号 + 高对比  
10. 失败恢复 + 双引擎兜底  

---

## 3. 部署前人工清单

### 代码与仓库

- [ ] `git status` 干净（忽略本地截图缓存即可）  
- [ ] 确认最近提交作者只有你本人  
- [ ] 推送到 GitHub `main`  
- [ ] Root Directory = `silentbridge-demo`  

### Vercel

- [ ] 按 `silentbridge-demo/DEPLOY.md` 导入  
- [ ] 配置 `ZHIPU_API_KEY`（可选；无 key 会规则兜底）  
- [ ] 部署成功后打开公网链接  

### 线上验收（部署后立刻做）

- [ ] Landing 可打开  
- [ ] 一键演示 60s 跑通  
- [ ] `/api/health` 返回 ok  
- [ ] 有 key 时显示 AI 实时整理；无 key 时离线整理可用  
- [ ] 手机窄屏无横向滚动  

### 比赛材料

- [ ] 公网 Demo 链接写入报名帖  
- [ ] 30s 录屏：一键演示 → 风险 → 保存  
- [ ] TRAE Session ID / 开发截图证据链  
- [ ] 报名帖场景文案与医院/药店/政务/问路一致  

---

## 4. 已知非 blocker（可复赛再做）

- 真机麦克风在部分浏览器仍不稳定（已有手动/演示兜底）  
- 自由文本抽取仍是规则引擎，复杂长句不如 LLM  
- 无障碍尚未做系统级读屏全量审计  
- 分享目前是复制文本，不是图片海报  

---

## 5. 建议操作顺序

1. 再做一次本地 `pnpm typecheck && pnpm --filter @silentbridge/web test && pnpm --filter @silentbridge/api test && pnpm build`  
2. 推送 GitHub  
3. Vercel 部署  
4. 线上 5 分钟验收  
5. 录屏 + 报名帖终稿  

**在完成第 2 步前，不必再改功能。**
